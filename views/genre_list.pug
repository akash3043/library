extends layout

block content
  h1=title
  ul
  each genre in genres
    li
      a(href=genre.url) #{genre.name}
  else
    li there are no genres available

  br
  a(href='/catalog/genre/create')
    Button.btn.btn-primary Create a new genre
